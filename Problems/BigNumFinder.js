const Bignumfinder = (num1, num2, num3, num4) =>{
    let Bignum = num1 >num2 ? num1 :num2
    Bignum = Bignum>num3
    Bignum = Bignum>num4}
    
    const Bignumfinders = (num1, num2, num3,num4)=>
    Math.max(num1,num2,num3,num4);

    console.log(`Bigger no is that ${122,123,124,125}`);


console.log(`Bigger number is ${12,13,14,15}`)
